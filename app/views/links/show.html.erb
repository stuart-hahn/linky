<h1>Link Show Page</h1>

<p>This link has <%= pluralize(@link.upvotes, 'upvote') %> and <%= pluralize(@link.points, 'total point') %>.</p>
<p>Read more: <%= link_to @link.title, @link.url %><p>
<p>Comments:<p>

<% if !@link.comments.empty? %>
    <ul>
    <% @link.comments.each do |comment| %>
        <li><%= comment.body %></li>
    <% end %>
    </ul>
<% end %>

<p>Add a comment:</p>
<%= render 'comments/form' %>

<% if current_user && current_user.owns_link?(@link) %>
    <%= link_to 'Edit', edit_link_path(@link) %> or 
    <%= link_to 'Delete', link_path(@link), method: :delete %>
<% end %>
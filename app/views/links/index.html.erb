<h1>All Links</h1>

<h2><%= @community.title if @community %></h2>

<% if @community %>
    <%= link_to  'Add a new ' + @community.title + ' link', new_community_link_path(@community) %>
<% else %>
    <%= link_to  "Add a new link", new_link_path %>
<% end %>

<%= render 'links' %>
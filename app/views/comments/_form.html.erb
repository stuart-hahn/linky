<%= form_with model: [@link, @comment], local: true do |f| %>
    <%= render 'shared/errors', object: @comment %>

    <%= f.label :body %>
    <%= f.text_area :body %>

    <p>This will be posted on: <%= @link.title %></p>
    <%= f.hidden_field :link_id %>

    <%= f.submit %>
<% end %>